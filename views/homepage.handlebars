HOME PAGE HANDLEBARS

{{#each posts as |post|}}
  <div class='row align-center mb-5'>
    <div class='col-md-6'>
      <h2>{{{get_emoji}}} {{post.username}}</h2>
    </div>
    <div class='col-md-6'>
      <h3><a href='mailto:{{post.email}}'>{{post.title}}</a></h3>
    </div>
  </div>
{{/each}}